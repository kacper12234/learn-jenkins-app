pipeline {
    agent any

        stages {

            stage('Build') {
                steps {
                    sh 'docker build -f ci/Dockerfile-playwright -t node_test_deploy_netlify .'
                    sh 'docker build -f ci/Dockerfile-aws-cli -t my-aws-cli .'
                }
            }
        }
}